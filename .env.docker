# Docker Environment Configuration
# This file is used when running NestJS application inside Docker containers
# or when connecting to Dockerized PostgreSQL from host machine

DB_HOST=skillbase-postgres
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=skillbase

# Application Configuration
PORT=3000
NODE_ENV=development

# JWT Configuration
JWT_SECRET=your-secret-key-change-in-production-please-use-a-strong-random-key
JWT_EXPIRES_IN=7d
